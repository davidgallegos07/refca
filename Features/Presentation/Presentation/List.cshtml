@model IEnumerable<PresentationWithTeachersDto>

@{
    ViewData["Title"] = "Ponencias";
}

<div id="wrapper">
    @await Html.PartialAsync("_TeacherBar")
    <!-- Navigation -->
    <div id="page-wrapper">
        <div class="row">
            <div class="col-lg-12">
                <h2 class="page-header">Ponencias</h2>
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        @ViewData["Title"]
                    </div>
                    <div class="panel-body">
                        <a asp-controller="Presentation" asp-action="New" class="btn btn-primary table-button">Nuevo <i class="fa fa-plus" aria-hidden="true"></i></a>
                        <table style="width:100%" class="table table-striped table-bordered table-hover" id="tableEntity">
                            <thead>
                                <tr>
                                    <th>Título</th>
                                    <th>Congreso</th>
                                    <th>Fecha de edición</th>
                                    <th>Autor(es)</th>
                                    <th>Archivo</th>
                                    <th>Estado</th>
                                    <th>Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var presentation in Model)
                                {
                                    <tr>
                                        <td>@presentation.Title</td>
                                        <td>@presentation.Congress</td>
                                        <td>@presentation.EditionDate.ToString("yyyy-MM-dd")</td>
                                        <td>
                                            @foreach (var teacher in presentation.TeacherPresentations)
                                            {
                                                <p>@teacher.Name</p>
                                            }
                                        </td>
                                        <td><a href="@presentation.PresentationPath">Descargar</a></td>
                                        <td>
                                            @if (presentation.IsApproved == true)
                                            {
                                                <span class="label label-success">Aprobado</span>
                                            }
                                            else
                                            {
                                                <span class="label label-warning">No aprobado</span>
                                            }
                                        </td>
                                        <td>
                                            <a asp-controller="Presentation" asp-action="Edit" asp-route-id="@presentation.Id" class="btn btn-info"
                                               data-toggle="tooltip" title="Editar"><i class="fa fa-pencil" aria-hidden="true"></i></a>

                                            <form asp-controller="Presentation" asp-action="Delete" method="post" asp-route-id="@presentation.Id" class="js-delete inline">
                                                <button type="submit" class="btn btn-danger" data-toggle="tooltip" title="Eliminar">
                                                    <i class="fa fa-trash-o" aria-hidden="true"></i>
                                                </button>
                                            </form>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    @{ await Html.RenderPartialAsync("_AdministrationList"); }
    @{ await Html.RenderPartialAsync("_DataTable"); }
}